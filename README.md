# github-proxy
web proxy
# 🌐 GitHub Web Proxy

Бесплатный веб-прокси для обхода блокировок через GitHub Pages.

## 🚀 Быстрый старт

1. **Скопируйте этот репозиторий**
   ```bash
   git clone https://github.com/cpidevpy/github-proxy.git
   ```

2. **Загрузите файлы в свой репозиторий**

3. **Включите GitHub Pages:**
   - Зайдите в Settings → Pages
   - Source: Deploy from branch
   - Branch: main, folder: / (root)
   - Сохраните

4. **Откройте ваш прокси:**
   ```
   https://cpidevpy.github.io/github-proxy/
   ```

## 📁 Структура проекта

```
github-proxy/
├── index.html          # Главная страница
├── proxy.html          # Прокси-браузер
├── script.js           # Логика прокси
├── .nojekyll           # Отключает Jekyll обработку
└── README.md           # Эта документация
```

## ⚙️ Настройка

### Свой CORS прокси
1. Создайте файл `config.js`:
```javascript
window.CUSTOM_PROXIES = {
    'my-proxy': 'https://ваш-прокси.herokuapp.com/?url='
};
```

2. Добавьте в `index.html`:
```html
<script src="config.js"></script>
```

### Использование своего домена
1. Создайте файл `CNAME`:
```
ваш-домен.com
```

2. Настройте DNS записи у регистратора

## 🔧 Технические детали

### Как это работает
1. Пользователь вводит URL
2. JavaScript отправляет запрос через CORS прокси
3. Полученный HTML обрабатывается и вставляется в iframe
4. Все ссылки перехватываются и перенаправляются через прокси

### Поддерживаемые прокси серверы
- ✅ CORS Anywhere
- ✅ AllOrigins
- ✅ CORS Proxy.io
- ✅ ThingProxy
- ✅ CodeTabs Proxy

## ⚠️ Ограничения

### Работает с:
- Статичные сайты (Wikipedia, документация)
- Новостные сайты (текстовый контент)
- Блоги и форумы
- Техническая документация

### Не работает с:
- YouTube и видеосервисы
- Социальные сети
- Банки и платежные системы
- Сайты с авторизацией
- WebSocket приложения

## 📱 PWA поддержка

Установите как приложение:
1. Откройте в Chrome/Edge
